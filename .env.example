# Local dev. Copy to .env — .env is gitignored, never commit real secrets.
DATABASE_URL=postgresql+psycopg://cabinet:cabinet@localhost:5432/cabinet
APP_PASSWORD=changeme
SESSION_SECRET=dev-only-not-secret
